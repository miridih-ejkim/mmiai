/**
 * MCPServer 생성 — Legacy (사용되지 않음)
 *
 * Lazy MCP Loading 도입으로 MCPServer 인스턴스는 더 이상 생성하지 않습니다.
 * 도구는 McpConnectionManager.getToolsets()로 요청 시점에 동적으로 로드됩니다.
 *
 * 이 파일은 참조용으로 유지됩니다.
 */
export {};
