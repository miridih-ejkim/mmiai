# ============================================================
# MMIAI 환경변수 (.env)
#
# 사용법:
#   cp .env.example .env
#   # .env 파일의 값들을 실제 키로 채우세요
#   docker compose up --build
#
# DATABASE_URL, MASTRA_SERVER_URL은
# docker-compose.yml에서 자동 override되므로 수정 불필요합니다.
# ============================================================

# ===== [필수] LLM =====
ANTHROPIC_API_KEY=sk-ant-...

# ===== Database (docker-compose가 자동 설정) =====
DATABASE_URL=postgresql://miai:password@localhost:5432/miai

# ===== [선택] MCP: Atlassian (Confluence + Jira) =====
# 미설정 시 Atlassian Agent가 비활성화됩니다.
# Option A: Streamable HTTP (Kubernetes 배포 시)
# MCP_ATLASSIAN_URL=http://atlassian-mcp:8080/mcp
# MCP_AUTH_TOKEN=optional-bearer-token
# Option B: stdio (로컬, pip install mcp-atlassian 필요)
CONFLUENCE_URL=https://your-domain.atlassian.net/wiki
CONFLUENCE_USERNAME=your-email@example.com
CONFLUENCE_API_TOKEN=your-confluence-api-token
JIRA_URL=https://your-domain.atlassian.net
JIRA_USERNAME=your-email@example.com
JIRA_API_TOKEN=your-jira-api-token

# ===== [선택] MCP: DataHub =====
# 미설정 시 DataHub Agent가 비활성화됩니다.
# Option A: Streamable HTTP (Kubernetes 배포 시)
# MCP_DATAHUB_URL=http://datahub-mcp:8080/mcp
# Option B: stdio (로컬, pip install mcp-server-datahub 필요)
DATAHUB_GMS_URL=https://datahub.example.com/api/gms
DATAHUB_GMS_TOKEN=your-datahub-token

# ===== [선택] MCP: Google Search =====
# 미설정 시 Google Search Agent가 비활성화됩니다.
GOOGLE_API_KEY=your-google-api-key
GOOGLE_SEARCH_ENGINE_ID=your-search-engine-id

